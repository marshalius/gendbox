from setuptools import setup

setup(
    name='mypackage',
    version='0.1.0',
    description='Açıklama',
    author='Your Name',
    packages=['mypackage'],
    install_requires=[
        'numpy',
        'pandas'
    ],
)